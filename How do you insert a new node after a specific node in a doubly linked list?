#include <iostream>

struct Node {
    int data;
    Node* prev;
    Node* next;
};

int main() {
    Node* head = nullptr;
    Node* newNode = new Node;
    newNode->data = 42;
    newNode->prev = nullptr;
    newNode->next = nullptr;
    head = newNode;
    return 0;
}
